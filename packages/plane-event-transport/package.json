{
	"name": "cyrus-plane-event-transport",
	"version": "0.1.0",
	"description": "Plane event transport: webhook receiver, translator, and issue tracker service for Plane",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"typecheck": "tsc --noEmit",
		"test": "vitest",
		"test:run": "vitest run"
	},
	"dependencies": {
		"@makeplane/plane-node-sdk": "0.2.11",
		"cyrus-core": "workspace:*",
		"fastify": "^5.2.0"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.3",
		"vitest": "^3.1.4"
	},
	"publishConfig": {
		"access": "public"
	}
}
